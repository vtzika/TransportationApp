package com.transportation.app;

import java.util.List;
import java.util.ArrayList;
import java.io.InputStream;
import java.io.IOException;
import java.lang.Exception;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.core.type.TypeReference;

public class TestDataReader {
/*
	TestDataReader() {
		ObjectMapper mapper = new ObjectMapper();
		List<Journey> journeys = new ArrayList<Journey>();
		try {
			InputStream inputStream = TypeReference.class.getResourceAsStream("../resources/testData.json");
			System.out.println(inputStream);
			// List<Journey> journeys = mapper.readValue(inputStream, typeReference);
			// JourneyService.save(users);
			System.out.println("Journey Saved!");
		} catch (IOException e) {
			System.out.println("Unable to save journey: " + e.getMessage());
		} catch (Exception e) {
			System.out.println("Unexcepted Exception");
			e.printStackTrace();
		}
	}
	*/

}
